machine:
  hosts:
    otcmcd1: 127.0.0.1
  services:
    - docker

dependencies:
  pre:
    - sudo add-apt-repository ppa:duggan/bats --yes
    - sudo add-apt-repository ppa:qpid/released  --yes
    - sudo apt-get update -q
    - sudo apt-get install -q libsasl2-2 libsasl2-modules libsasl2-modules-db bats qpid-send qpid-receive

test:
  override:
    - docker build -t ecmi/mc:sim .
    - bats tests.bats

deployment:
  docker:
    branch: /.*/
    commands:
      - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
      - docker tag -f ecmi/mc:sim docker.io/ecmi/mc:sim
      - docker tag -f ecmi/mc:sim docker.io/ecmi/mc:latest
      - docker push ecmi/mc:sim
      - docker push ecmi/mc:latest
